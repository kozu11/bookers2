<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/user-info', user: @user %>
      <%= render 'books/user-info', book: @book %>
    </div>
    
    <div class="col-md-8 offset-md-1">
      <h3>Groups</h3>
      <table class="table">
        <thead>
          <th></th>
          <th>グループ名</th>
          <th>紹介文</th>
          <th></th>
        </thead>
        <% @groups.each do |group| %>
        <tbody>
          <td><%= attachment_image_tag group, :image, :fill, 20, 20, fallback: "no_image.jpg", size: "20x20" %></td>
          <td><%= link_to group_path(group) do %><%= group.name %><% end %></td>
          <td><%= group.introduction %></td>
          <!--#ログイン中のuserがオーナーだった場合のみ表示-->
          <% if group.owner_id == current_user.id %>
            <td><%= link_to "Edit", edit_group_path(group), class: "btn btn-success" %></td>
          <% end %>
        </tbody>
        <% end %>
      </table>
      
    </div>
  </div>
</div>